# Proyecto crerado con el comando npx create-react-app clima

Para correrlo desde el directorio:

### `npm start`

Se correrá en modo desarrollo en [http://localhost:3000](http://localhost:3000) para ver en el navegador.

